<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>sign up</title>
</head>
<body>
    <form action="/sign-up" method="POST">
        <input type="text" name="username" placeholder="username">
        <% if(errors.username) {%>
            <p><%= errors.username%></p>
            <%}%>
        <input type="text" name="password" placeholder="password">
        <input type="text" name="cnfrmPassword" placeholder="confirm password">
        <% if(errors.password) {%>
            <p><%= errors.password%></p>
            <%}%>
        <button type="submit">sign up</button>
    </form>
    <a href="/">back</a>
</body>
</html>